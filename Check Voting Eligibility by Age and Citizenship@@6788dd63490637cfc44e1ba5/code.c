#include <stdio.h>
int main() {
    int age,stat;
    scanf("%d" "%d",&a,&b);
    if(a>=18 && b == 1){
        printf("Eligible");
    }
    else{
        printf("Not Eligible");
    }
    return 0;
}